<div class="header">
  <h1><%= @user.name %></h1>
  <%= link_to "Logout", logout_path, class: "logout-btn" %>
</div>

<div class="stats">
  <!-- Week Card -->
  <div class="stat-card-large">
    <div class="card-header">
      <h2>Week <%= @selected_week %></h2>
      <select id="weekSelector" class="week-selector-small">
        <% (1..18).each do |week_num| %>
          <option value="<%= week_num %>" <%= 'selected' if week_num == @selected_week %>>Week <%= week_num %></option>
        <% end %>
      </select>
    </div>
    <div class="stat-row">
      <div class="stat-item">
        <div class="stat-label">Record</div>
        <div class="stat-value"><%= @week_record %></div>
      </div>
      <div class="stat-item">
        <div class="stat-label">Winnings</div>
        <div class="stat-value <%= @week_winnings >= 0 ? 'positive' : 'negative' %>">
          <%= format_currency(@week_winnings) %>
        </div>
      </div>
      <div class="stat-item">
        <div class="stat-label">Spent</div>
        <div class="stat-value"><%= format_currency(@week_spent) %></div>
      </div>
    </div>
  </div>
  
  <!-- Season Card -->
  <div class="stat-card-large">
    <div class="card-header">
      <h2>Season</h2>
    </div>
    <div class="stat-row">
      <div class="stat-item">
        <div class="stat-label">Record</div>
        <div class="stat-value"><%= @season_record %></div>
      </div>
      <div class="stat-item">
        <div class="stat-label">Winnings</div>
        <div class="stat-value <%= @season_winnings >= 0 ? 'positive' : 'negative' %>">
          <%= format_currency(@season_winnings) %>
        </div>
      </div>
      <div class="stat-item">
        <div class="stat-label">Spent</div>
        <div class="stat-value"><%= format_currency(@season_spent) %></div>
      </div>
    </div>
  </div>
</div>

<script>
  document.getElementById('weekSelector')?.addEventListener('change', function() {
    const url = new URL(window.location.href);
    url.searchParams.set('week', this.value);
    window.location.href = url.toString();
  });
</script>

<div class="history-section">
  <h2>Betting History</h2>
  <% if @betting_histories.any? %>
    <% @betting_histories.each do |bet| %>
      <div class="bet-item">
        <div class="bet-details">
          <%= bet.formatted_description %>
        </div>
        <div class="bet-footer">
          <span class="bet-line"><%= bet.formatted_line %></span>
          <span class="bet-stake"><%= format_currency(bet.total_stake) %></span>
          <span class="bet-result <%= bet.result %>">
            <%= bet.result.humanize %>
          </span>
        </div>
        <% if bet.winnings > 0 %>
          <div class="individual-winnings">+<%= format_currency(bet.winnings) %></div>
        <% end %>
      </div>
    <% end %>
  <% else %>
    <div class="empty-state">No betting history yet.</div>
  <% end %>
</div>

<style>
  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    margin: 0;
    padding: 0;
    background: #111;
    color: white;
  }

  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
    background: #1a1a1a;
    border-bottom: 1px solid #333;
  }

  .header h1 {
    margin: 0;
    color: white;
  }

  .logout-btn {
    background: #d32f2f;
    color: white;
    text-decoration: none;
    padding: 8px 16px;
    border-radius: 4px;
    font-size: 14px;
  }

  .stats {
    padding: 16px;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .stat-card-large {
    background: #1a1a1a;
    border-radius: 12px;
    padding: 20px;
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
    padding-bottom: 12px;
    border-bottom: 1px solid #333;
  }

  .card-header h2 {
    margin: 0;
    font-size: 20px;
    color: white;
  }

  .week-selector-small {
    padding: 6px 12px;
    background: #212121;
    color: white;
    border: 1px solid #333;
    border-radius: 6px;
    font-size: 12px;
    cursor: pointer;
  }

  .week-selector-small:focus {
    outline: none;
    border-color: #4d9640;
  }

  .stat-row {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }

  .stat-item {
    text-align: center;
  }

  .stat-card {
    background: #1a1a1a;
    border-radius: 8px;
    padding: 16px;
  }

  .stat-label {
    font-size: 13px;
    color: #888;
    margin-bottom: 8px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .stat-value {
    font-size: 20px;
    font-weight: bold;
    color: white;
  }

  .stat-value.positive {
    color: #4d9640;
  }

  .stat-value.negative {
    color: #d32f2f;
  }

  .history-section {
    padding: 16px;
  }

  .history-section h2 {
    margin: 0 0 16px 0;
    font-size: 18px;
  }

  .history-item {
    background: #1a1a1a;
    border-radius: 8px;
    padding: 12px;
    margin-bottom: 12px;
    position: relative;
  }

  .individual-winnings {
    position: absolute;
    top: 12px;
    right: 12px;
    color: #4d9640;
    font-weight: bold;
    font-size: 14px;
  }

  .history-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 8px;
    font-size: 12px;
  }

  .history-date, .history-week {
    color: #888;
  }

  .bet-result {
    padding: 2px 8px;
    border-radius: 8px;
    font-size: 11px;
    text-transform: uppercase;
  }

  .bet-result.pending {
    background: #333;
    color: #888;
  }

  .bet-result.win {
    background: #4d9640;
    color: white;
  }

  .bet-result.loss {
    background: #d32f2f;
    color: white;
  }

  .bet-result.push {
    background: #ff9800;
    color: white;
  }

  .history-bet {
    font-size: 15px;
    margin-bottom: 8px;
  }

  .history-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 8px;
    font-size: 14px;
  }

  .history-line {
    color: #888;
  }

  .history-stake {
    color: #888;
  }

  .history-pl.positive {
    color: #4d9640;
  }

  .history-pl.negative {
    color: #d32f2f;
  }

  .empty-state {
    text-align: center;
    color: #888;
    padding: 40px 20px;
  }


  .bet-item {
    background: #1a1a1a;
    border-radius: 8px;
    padding: 16px;
    margin: 16px 0;
    position: relative;
  }

  .individual-winnings {
    position: absolute;
    top: 12px;
    right: 12px;
    color: #4d9640;
    font-weight: bold;
    font-size: 14px;
  }

  .bet-user {
    font-size: 14px;
    color: #888;
    margin-bottom: 8px;
  }

  .bet-details {
    font-size: 16px;
    margin-bottom: 8px;
  }

  .bet-line {
    color: #4d9640;
    margin-left: 8px;
  }

  .bet-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .bet-stake {
    color: #888;
    font-size: 14px;
  }

  .bet-result {
    display: inline-block;
    padding: 4px 12px;
    border-radius: 12px;
    font-size: 12px;
    text-transform: uppercase;
  }

  .bet-result.pending {
    background: #333;
    color: #888;
  }

  .bet-result.win {
    background: #4d9640;
    color: white;
  }

  .bet-result.loss {
    background: #d32f2f;
    color: white;
  }

  .bet-result.push {
    background: #ff9800;
    color: white;
  }

  .empty-state {
    text-align: center;
    color: #888;
    padding: 40px 20px;
  }
</style>
