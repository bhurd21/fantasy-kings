<div class="header">
  <h1>Week <%= @week %></h1>
  <select id="weekSelector" class="week-selector">
    <% (1..28).each do |week_num| %>
      <option value="<%= week_num %>" <%= 'selected' if week_num == @week %>>Week <%= week_num %></option>
    <% end %>
  </select>
</div>

<div class="bets-list">
  <% if @user_bets.any? %>
    <% @user_bets.each_with_index do |(user, bets), index| %>
      <div class="user-group">
        <%= link_to user_profile_path(user), class: "user-group-header-link" do %>
          <div class="user-group-header">
            <div class="user-header-grid">
              <div class="user-info">
                <span class="user-rank rank-<%= index + 1 %>">#<%= index + 1 %></span>
                <span class="user-name"><%= user.display_name %></span>
              </div>
              <div class="user-line-placeholder"></div>
              <div class="user-spent"><%= format_currency(@user_stats[user.id][:total_spent]) %></div>
              <div class="user-winnings <%= @user_stats[user.id][:total_winnings] > 0 ? 'positive' : '' %>">
                <%= format_currency(@user_stats[user.id][:total_winnings]) %>
              </div>
            </div>
          </div>
        <% end %>
        
        <% bets.each do |bet| %>
          <div class="bet-item">
            <% if bet.dk_game&.commence_time %>
              <div class="bet-commence-time"><%= format_bet_commence_time_compact(bet.dk_game.commence_time) %></div>
            <% end %>
            <div class="bet-grid">
              <div class="bet-description">
                <%= sport_icon(bet.dk_game, bet.result, bet) %>
                <%= bet.formatted_description %>
              </div>
              <div class="bet-line"><%= bet.formatted_line %></div>
              <div class="bet-stake"><%= format_currency(bet.total_stake) %></div>
              <div class="bet-outcome <%= bet.result %>">
                <% if bet.result == 'pending' %>
                  <%= bet.result.humanize %>
                <% elsif bet.winnings > 0 %>
                  +<%= format_currency(bet.winnings) %>
                <% else %>
                  <%= format_currency(0) %>
                <% end %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
  <% else %>
    <div class="empty-state">No bets placed for this week yet.</div>
  <% end %>
</div>

<script>
  document.getElementById('weekSelector').addEventListener('change', function() {
    window.location.href = '/week/' + this.value;
  });
</script>

<style>
  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    margin: 0;
    padding: 0;
    background: #111;
    color: white;
  }

  .header {
    padding: 20px;
    background: #1a1a1a;
    border-bottom: 1px solid #333;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .header h1 {
    margin: 0;
    color: white;
  }

  .week-selector {
    padding: 8px 16px;
    background: #212121;
    color: white;
    border: 1px solid #333;
    border-radius: 6px;
    font-size: 14px;
    cursor: pointer;
  }

  .week-selector:focus {
    outline: none;
    border-color: #4d9640;
  }

  .bets-list {
    padding: 16px;
  }

  .user-group {
    margin-bottom: 24px;
    border: 1px solid #333;
    border-radius: 8px;
  }

  .user-group-header-link {
    text-decoration: none;
    color: inherit;
    display: block;
    transition: transform 0.2s;
  }

  .user-group-header-link:active {
    transform: scale(0.98);
  }

  .user-group-header {
    background: #212121;
    padding: 8px;
    border-radius: 8px;
    cursor: pointer;
  }

  .user-group-header:hover {
    background: #2a2a2a;
  }

  .user-header-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 0.8fr 1fr;
    gap: 16px;
    align-items: center;
  }

  .user-info {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .user-rank {
    font-size: 18px;
    font-weight: bold;
    color: #555;
    min-width: 35px;
  }

  .user-rank.rank-1 {
    color: #FFD700;
    font-size: 20px;
  }

  .user-rank.rank-2 {
    color: #C0C0C0;
    font-size: 19px;
  }

  .user-rank.rank-3 {
    color: #CD7F32;
    font-size: 19px;
  }

  .user-name {
    font-size: 16px;
    font-weight: bold;
    color: white;
  }

  .user-line-placeholder {
    /* Empty placeholder to align with bet line column */
  }

  .user-spent {
    color: #888;
    font-size: 14px;
    font-weight: bold;
    text-align: right;
  }

  .user-winnings {
    color: #888;
    font-size: 14px;
    font-weight: bold;
    text-align: right;
  }

  .user-winnings.positive {
    color: #4d9640;
    font-weight: bold;
  }

  .bet-item {
    background: #1a1a1a;
    border-radius: 8px;
    padding: 4px;
    margin-top: 8px;
  }

  .bet-grid {
    display: grid;
    grid-template-columns: 8fr 2fr 2fr 3fr;
    padding: 4px;
    gap: 16px;
    align-items: center;
  }

  .bet-commence-time {
    font-size: 11px;
    color: #666;
  }

  .bet-description {
    font-size: 16px;
    color: white;
  }

  .sport-icon {
    font-size: 14px;
    margin-right: 2px;
    vertical-align: middle;
  }

  .sport-icon-pending {
    color: #888;
  }

  .sport-icon-win {
    color: #4d9640;
  }

  .sport-icon-loss {
    color: #d32f2f;
  }

  .sport-icon-push {
    color: #ff9800;
  }

  .bet-line {
    color: #888;
    font-size: 14px;
    text-align: right;
  }

  .bet-stake {
    color: #888;
    font-size: 14px;
    text-align: right;
  }

  .bet-outcome {
    font-size: 14px;
    text-align: right;
    font-weight: bold;
  }

  .bet-outcome.pending {
    color: #888;
    text-transform: uppercase;
    font-size: 12px;
  }

  .bet-outcome.win {
    color: #4d9640;
  }

  .bet-outcome.loss {
    color: #d32f2f;
  }

  .bet-outcome.push {
    color: #ff9800;
  }

  .bet-result {
    display: inline-block;
    padding: 4px 12px;
    border-radius: 12px;
    font-size: 12px;
    text-transform: uppercase;
  }

  .bet-result.pending {
    background: #333;
    color: #888;
  }

  .bet-result.win {
    background: #4d9640;
    color: white;
  }

  .bet-result.loss {
    background: #d32f2f;
    color: white;
  }

  .bet-result.push {
    background: #ff9800;
    color: white;
  }

  .empty-state {
    text-align: center;
    color: #888;
    padding: 40px 20px;
  }
</style>
